version: "3.9"

services:
  dashboard:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - catalog_data:/app/budget_dashboard/catalog
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1

volumes:
  catalog_data:
