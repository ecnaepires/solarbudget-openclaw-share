# Development and testing dependencies — not needed to run the dashboard.
# Install with: pip install -r requirements-dev.txt
#
# Requires the main lockfile first: pip install -r requirements.txt

pytest>=7.0
pytest-cov>=4.0
